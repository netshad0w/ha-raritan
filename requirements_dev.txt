homeassistant==2026.7.1
raritan==4.3.13.52458
# Runtime deps of HA's core `dhcp` component, which our manifest's dhcp
# matchers pull in. Bundled in real HA installs; pinned here so the dev
# container / venv can load it and exercise DHCP discovery (versions match
# homeassistant 2026.7.1's dhcp manifest).
aiodhcpwatcher==1.2.7
aiodiscover==3.3.2
cached-ipaddress==1.1.2
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-homeassistant-custom-component==0.13.345
ruff==0.15.20
mypy==2.1.0
